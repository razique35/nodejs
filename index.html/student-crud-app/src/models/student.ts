class Student {
    name: string;
    fatherName: string;
    rollNumber: number;
    class: string;

    constructor(name: string, fatherName: string, rollNumber: number, class: string) {
        this.name = name;
        this.fatherName = fatherName;
        this.rollNumber = rollNumber;
        this.class = class;
    }

    // Additional methods for data validation or transformation can be added here
}